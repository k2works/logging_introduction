LoadModule passenger_module /usr/local/rvm/gems/ruby-1.9.3-p547@webapp/gems/passenger-4.0.50/buildout/apache2/mod_passenger.so
<IfModule mod_passenger.c>
  PassengerRoot /usr/local/rvm/gems/ruby-1.9.3-p547@webapp/gems/passenger-4.0.50
  PassengerDefaultRuby /usr/local/rvm/gems/ruby-1.9.3-p547@webapp/wrappers/ruby
</IfModule>

# Deploying a Ruby on Rails application: an example

# Suppose you have a Rails application in /somewhere. Add a virtual host to
# your Apache configuration file and set its DocumentRoot to /somewhere/public:
#

RackEnv development

<VirtualHost *:80>
   ServerName www.webapp.com
   DocumentRoot /var/www/my_store/public
   <Directory /var/www/my_store/public>
      Allow from all
      Options -MultiViews
   </Directory>
</VirtualHost>
